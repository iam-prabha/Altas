# Lesson 2 Complete: Functions, Union Types & Control Flow

Demonstrated understanding of union types, discriminated unions, and type narrowing through the order status formatter exercise, including the `parseInput` bonus.

## Demonstrated understanding
- Discriminated union with `status` literal field and `switch` narrowing
- Exhaustiveness check via `never` default case
- `typeof` narrowing for primitive unions (`string | number`)
- Optional/default/rest parameter signatures

## Implications
- Ready for advanced narrowing patterns and utility types in future lessons
- Discriminated union pattern is understood — can reference in future exercises without re-teaching
