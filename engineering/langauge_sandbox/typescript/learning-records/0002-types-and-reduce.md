# Types, Inference, and Array.reduce

Completed Lesson 1 (types and inference). User implemented the cart total exercise and independently experimented with a second `reduce` call on a separate array.

## Demonstrated understanding
- Writes typed function signatures correctly (`items: CartItem[]` param, `: number` return)
- Uses `reduce` to sum array values — both the exercise and an additional self-directed example
- No use of `any`
- Comprehends type inference (didn't annotate `total_price` — let TS infer it)

## Implications
- Ready for Lesson 2: functions, union types, and control flow
- Showed appetite for experimenting beyond the exercise — can include stretch goals in future lessons
