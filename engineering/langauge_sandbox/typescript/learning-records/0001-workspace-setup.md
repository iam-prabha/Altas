# Workspace Initiated: TypeScript for Application Development

Teaching workspace established at `engineering/langauge_sandbox/typescript/` with the mission to build any application using TypeScript. User prefers a steeper learning curve — JS concepts are taught embedded in TypeScript, not as a prerequisite.

## Prior knowledge disclosed
- Comfortable starting directly with TypeScript, learning JS concepts alongside
- No prior TypeScript exposure claimed
- Available runtimes: Node 25, Bun, Deno

## Implications
- First lesson jumps straight into typed functions with basic primitives — no JS fundamentals primer needed
- The `src/` directory serves as the practice workspace; exercises produce real `.ts` files
- Shared assets (styles.css, quiz.js) established for reuse across future lessons
